/**
 * Route Constants
 *
 * Define all application routes in one place to maintain consistency.
 */

export const ROUTES = {
  HOME: '/',
  LOGIN: '/login',
  PROFILE: '/profile',
  USERS: '/users',
  NOT_FOUND: '*',
};
